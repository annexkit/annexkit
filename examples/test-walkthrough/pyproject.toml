[project]
name = "annexkit-test-walkthrough"
version = "0.1.0"
description = "End-to-end test walkthrough: three paying-customer personas → Annex IV PDFs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "annexkit",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv.sources]
annexkit = { path = "../../sdk", editable = true }

[tool.uv]
package = false
